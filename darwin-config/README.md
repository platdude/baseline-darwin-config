# Nix-darwin

This flake.nix file's purpose is to serve as a starting point for MacOS configuration, that can then easily extended with other nix files, by using extra darwinConfig settings or Home Manager.